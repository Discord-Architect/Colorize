import { Middleware, BaseMiddleware } from '@discord-architect/core'

@Middleware('app::ready')
export default class $className implements BaseMiddleware {
	public async run(...args: any): Promise<void> {
		// Your code here
	}
}
