import { BasePrerequisite, Prerequisite, PrerequisiteContext, PrerequisiteContext } from '@discord-architect/core'

@Prerequisite({ name: '' })
export default class $className implements BaseRequire {
	public async isValid(context: PrerequisiteContext): Promise<boolean> {
		// Your code here
	}
}
